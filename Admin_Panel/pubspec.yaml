name: pequire_admin_panel
description: "Pequire Administrative Dashboard"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.1.0
  firebase_core: ^2.25.0
  cloud_firestore: ^4.15.1
  firebase_auth: ^4.17.0
  google_fonts: ^6.1.0
  fl_chart: ^0.66.0 # For analytics charts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/lOGO (1).png
    - assets/Wordmark.png
